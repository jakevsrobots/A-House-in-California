package california.sprites {
    import org.flixel.*;
    import california.Main;

    public class Bird extends GameSprite {
        public function Bird(name:String, X:Number, Y:Number):void {
            super(name, X, Y);
        }
    }
}